# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Note: pubspec.lock should be committed for applications (not packages)
# to ensure consistent dependency versions across all developers.
# The *.lock pattern below is commented out for this reason.
# *.lock

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Android Gradle build cache (NOT the wrapper files - those should be committed)
android/.gradle/
**/android/.gradle/

# Android local configuration
android/local.properties
**/local.properties

# Firebase configuration files
# Note: google-services.json contains Firebase config. While API keys in this file
# are safe to expose (they're meant for client apps), many teams prefer to keep
# this file out of version control. Uncomment the line below if you want to ignore it.
android/app/google-services.json
**/google-services.json
**/GoogleService-Info.plist
firebase_app_id_file.json

# Android signing keys (NEVER commit these!)
*.keystore
*.jks
key.properties
**/key.properties

# iOS Pods
ios/Pods/
**/ios/Pods/
ios/Podfile.lock
**/ios/Podfile.lock

# Environment files
.env
.env.local
.env.*.local
